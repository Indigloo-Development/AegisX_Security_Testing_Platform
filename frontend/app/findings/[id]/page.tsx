'use client';
import {useEffect,useState} from 'react';
import {useParams,useRouter} from 'next/navigation';
import AppShell from '../../../components/AppShell';
import {api,API} from '../../../lib/api';

export default function FindingPage(){const params=useParams();const router=useRouter();const id=String(params.id);const [f,setF]=useState<any>(null);const [form,setForm]=useState<any>(null);const [saving,setSaving]=useState(false);const [err,setErr]=useState('');
useEffect(()=>{api(`/api/console/findings/${id}`).then(x=>{setF(x);setForm(x)}).catch(e=>setErr(e.message))},[id]);
if(err)return <AppShell><div className="notice error">{err}</div></AppShell>; if(!f||!form)return <AppShell><div className="boot-card">Loading finding…</div></AppShell>;
async function save(){setSaving(true);try{const updated=await api(`/api/console/findings/${id}`,{method:'PATCH',body:JSON.stringify(form)});const next=await api(`/api/console/findings/${id}`);setF(next);setForm(next)}catch(e:any){setErr(e.message)}finally{setSaving(false)}}
async function remove(){if(!confirm('Delete this finding?'))return;await api(`/api/console/findings/${id}`,{method:'DELETE'});router.push('/findings')}
const refs=Array.isArray(f.references)?f.references:[];return <AppShell><div className="page-stack"><section className="hero-strip"><div><span className="eyebrow">FINDING DETAIL</span><h2>AX-{f.id} · {f.title}</h2><p>{f.endpoint||'No affected endpoint captured'}</p></div><div className="action-row"><button className="btn ghost" onClick={()=>router.back()}>← Back</button><button className="btn danger" onClick={remove}>Delete</button></div></section>{err&&<div className="notice error">{err}</div>}<section className="panel"><div className="detail-grid six"><div><span>Issue ID</span><b>AX-{f.id}</b></div><div><span>Severity</span><b className={`sev ${f.severity}`}>{f.severity}</b></div><div><span>CVSS V4.0</span><b>{f.cvss_v4||'Not calculated'}</b></div><div><span>CVSS Source</span><b>{f.cvss_source||'unknown'}</b></div><div><span>CWE-ID</span><b>{f.cwe||'Not mapped'}</b></div><div><span>OWASP</span><b>{f.owasp_mapping||'Not mapped'}</b></div></div><div className="detail-grid four lower"><div><span>Risk Category</span><b>{f.severity}</b></div><div><span>Affected URL</span><b>{f.endpoint||'—'}</b></div><div><span>Affected Parameter</span><b>{f.affected_parameter||'—'}</b></div><div><span>Affected Component</span><b>{f.affected_component||f.category}</b></div></div></section><section className="panel"><div className="panel-head"><h3>Editable Finding Record</h3><span className="muted">All fields are persisted and reflected in Findings, Issue Track Base and reports.</span></div><div className="form-grid two"><label className="field"><span>Issue Name</span><input value={form.title||''} onChange={e=>setForm({...form,title:e.target.value})}/></label><label className="field"><span>Severity</span><select value={form.severity} onChange={e=>setForm({...form,severity:e.target.value})}>{['critical','high','medium','low','info'].map(x=><option key={x}>{x}</option>)}</select></label><label className="field"><span>Status</span><select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}>{['open','closed','risk_accepted','ignored'].map(x=><option key={x}>{x}</option>)}</select></label><label className="field"><span>Verification</span><select value={form.verification||'unreviewed'} onChange={e=>setForm({...form,verification:e.target.value})}>{['reviewed','unreviewed'].map(x=><option key={x}>{x}</option>)}</select></label><label className="field"><span>Classification</span><select value={form.classification||'need_further_investigate'} onChange={e=>setForm({...form,classification:e.target.value})}>{['false_positive','exploitable','need_further_investigate'].map(x=><option key={x}>{x.replace(/_/g,' ')}</option>)}</select></label><label className="field"><span>CVSS V4.0 Score</span><input value={form.cvss_v4||''} onChange={e=>setForm({...form,cvss_v4:e.target.value})}/></label><label className="field"><span>CVSS V4.0 Vector</span><input value={form.cvss_vector_v4||''} onChange={e=>setForm({...form,cvss_vector_v4:e.target.value})}/></label><label className="field"><span>CWE-ID</span><input value={form.cwe||''} onChange={e=>setForm({...form,cwe:e.target.value})}/></label><label className="field wide"><span>Affected URL / Endpoint</span><input value={form.endpoint||''} onChange={e=>setForm({...form,endpoint:e.target.value})}/></label><label className="field"><span>Affected Parameter</span><input value={form.affected_parameter||''} onChange={e=>setForm({...form,affected_parameter:e.target.value})}/></label><label className="field"><span>Affected Component</span><input value={form.affected_component||''} onChange={e=>setForm({...form,affected_component:e.target.value})}/></label><label className="field wide"><span>Test Payload</span><textarea value={form.test_payload||''} onChange={e=>setForm({...form,test_payload:e.target.value})}/></label><label className="field wide"><span>Issue Description</span><textarea value={form.description||''} onChange={e=>setForm({...form,description:e.target.value})}/></label><label className="field wide"><span>Business Impact</span><textarea value={form.business_impact||form.risk_reason||''} onChange={e=>setForm({...form,business_impact:e.target.value})}/></label><label className="field wide"><span>Remediation</span><textarea value={form.remediation||''} onChange={e=>setForm({...form,remediation:e.target.value})}/></label><label className="field wide"><span>References (one URL per line)</span><textarea value={(form.references||[]).join('\n')} onChange={e=>setForm({...form,references:e.target.value.split(/\n+/).map((x:string)=>x.trim()).filter(Boolean)})}/></label></div><button className="btn primary" disabled={saving} onClick={save}>{saving?'Saving…':'Save All Changes'}</button></section><section className="panel"><h3>Proof of Concept & Evidence</h3><div className="evidence-grid"><div><span>HTTP Request</span><pre>{f.http_request||'Not captured'}</pre></div><div><span>HTTP Response</span><pre>{f.http_response||'Not captured'}</pre></div><div><span>Test Payload</span><pre>{f.test_payload||'Not captured'}</pre></div></div>{f.screenshot&&<div className="detail-block"><b>Evidence Screenshot</b><p><a href={`${API}${f.screenshot}`} target="_blank" className="text-link">Open screenshot ↗</a></p><img src={`${API}${f.screenshot}`} alt="Finding evidence" className="evidence-image"/></div>}</section><section className="panel"><h3>Issue Description</h3><p className="long-copy">{f.description}</p><h3>Business Impact</h3><p className="long-copy">{f.business_impact||f.risk_reason||'Business impact requires contextual validation.'}</p><h3>References</h3><ul className="reference-list">{refs.map((r:any,i:number)=><li key={i}><a href={String(r)} target="_blank">{String(r)}</a></li>)}</ul></section></div></AppShell>}
